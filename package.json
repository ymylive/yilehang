{
  "name": "renling",
  "version": "1.0.0",
  "description": "韧翎成长计划",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @renling/unified-miniapp dev:h5",
    "dev:mp": "pnpm --filter @renling/unified-miniapp dev:mp-weixin",
    "dev:api": "cd apps/api && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "pnpm --filter @renling/unified-miniapp build:h5",
    "build:mp": "pnpm --filter @renling/unified-miniapp build:mp-weixin"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

